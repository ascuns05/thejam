extends ../layout

block content
  .container
    .row  
      ul.collapsible.collapsible-admin-view-posts
        each post in posts
          li
            .collapsible-header
              b #{post.title}
            .collapsible-body #{post.text}

